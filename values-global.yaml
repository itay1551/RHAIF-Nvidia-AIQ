# Generated by patternizer. secretStore.backend none: make load-secrets writes aiq-credentials.
global:
  pattern: aiq
  # false: install OpenShift GitOps via make install (default for clusters without Argo CD).
  # true: reuse an existing cluster Argo CD control plane.
  singleArgoCD: false
  secretLoader:
    disabled: false
  secretStore:
    backend: none
main:
  clusterGroupName: prod
  multiSourceConfig:
    enabled: true
    clusterGroupChartVersion: 0.9.*
